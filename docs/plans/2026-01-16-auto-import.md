# Auto-Import Logic\n\n> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.\n\n**Goal:** Automatically import existing credentials from `~/.config/opencode/auth.json` into Studio profiles so they appear in the UI without manual re-adding.\n\n**Architecture:**\n- **Startup Sync:** On server start (and `syncAntigravityPool`), check `auth.json`.\n- **Profile Creation:** If `auth.json` has credentials for a provider but no corresponding profile exists in `auth-profiles/`, create one.\n- **Naming:** Use email if available, else `imported-{timestamp}`.\n\n**Tech Stack:** Node.js fs\n\n---\n\n### Task 1: Implement Import Logic\n\n**Files:**\n- Modify: `server/index.js`\n\n**Step 1: `importExistingAuth` function**\n- Read `auth.json`.\n- Iterate providers (`google`, `openai`, `anthropic`, etc.).\n- Check if `activeProfiles[provider]` is set.\n- If not, check if `auth-profiles/{provider}` has files.\n- If no profiles, create one from `auth.json` content.\n- Set it as active.\n\n**Step 2: Hook into startup**\n- Call `importExistingAuth()` after `loadStudioConfig()`.\n\n---\n\n**Execution:**\n1. Implement `importExistingAuth`.\n2. Add call to `server/index.js`.
